import { FC, ReactElement } from 'react'
import { WhyUsContainer } from '@/components/whyus/WhyUsContainer'

export const WhyUsSection: FC = (): ReactElement => {
  return (
    <section>
      <WhyUsContainer />
    </section>
  )
}
